{% extends "_layout.twig" %}

{% block body %}

	<h3>Liste des utilisateurs</h3>
	<hr>

	<div class="mb-3">
		<a href="/admin/utilisateurs/nouveau" class="btn btn-primary">➕ Nouvel utilisateur</a>
	</div>

	<table class="table table-striped">
		<thead>
			<tr>
				<th>ID</th>
				<th>Nom</th>
				<th>Prénom</th>
				<th>Pseudo</th>
				<th>Email</th>
				<th>Rôle</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for utilisateur in utilisateurs %}
				<tr>
					<td>{{ utilisateur.id }}</td>
					<td>{{ utilisateur.nom }}</td>
					<td>{{ utilisateur.prenom }}</td>
					<td>{{ utilisateur.username }}</td>
					<td>{{ utilisateur.email }}</td>
					<td>{{ utilisateur.rolelabel }}</td>
					<td>
						<a href="/admin/utilisateurs/utilisateur-{{ utilisateur.id }}" class="btn btn-sm btn-primary">Voir</a>
						{% if utilisateur.username != 'admin' %}
						<form method="post" action="/admin/utilisateurs/utilisateur_supprimer-{{ utilisateur.id }}" style="display:inline;">
							<button type="submit" class="btn btn-sm btn-danger"
							        onclick="return confirm('Supprimer {{ utilisateur.prenom }} {{ utilisateur.nom }} ?')">
								Supprimer
							</button>
						</form>
						{% else %}
						<span class="badge bg-info">🔒 Protégé</span>
						{% endif %}
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}